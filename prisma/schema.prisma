// schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = "postgres://default:KqIWVxmAp1s6@ep-round-cake-a6gy4naz-pooler.us-west-2.aws.neon.tech/verceldb?pgbouncer=true&connect_timeout=15&sslmode=require"
  directUrl = "postgres://default:KqIWVxmAp1s6@ep-round-cake-a6gy4naz.us-west-2.aws.neon.tech/verceldb?sslmode=require"
}

model User {
  id        String    @id @default(uuid())
  username  String?
  email     String?
  createdAt DateTime  @default(now())
  sources   Source[]
  formulas  Formula[]
  styles    Style[]
  threads   Thread[]
}

model Source {
  id         String @id @default(uuid())
  name       String?
  type       String?
  category   String?
  title      String?
  text       String?
  embeddings Json[]
  userId     String
  user       User   @relation(fields: [userId], references: [id])
}

model Formula {
  id           String @id @default(uuid())
  title        String?
  instructions String?
  formula      String?
  thinkAbout   String?
  userId       String
  user         User   @relation(fields: [userId], references: [id])
}

model Style {
  id        String @id @default(uuid())
  title     String?
  examples  Json[]
  summary   String?
  blueprint String?
  sample    String?
  userId    String
  user      User   @relation(fields: [userId], references: [id])
}

model Thread {
  id             String   @id @default(uuid())
  created        DateTime @default(now())
  updated        DateTime @updatedAt
  initial_prompt String?
  messages       Json[]
  threads        Json?
  userId         String
  user           User     @relation(fields: [userId], references: [id])
}
